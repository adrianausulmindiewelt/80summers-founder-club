/**
 * funnel-config.js — Zentrale Frontend-Konfiguration für den Quiz-Funnel.
 *
 * Diese Datei wird VOR funnel-init.js geladen.
 * Trage hier deine Tracking-IDs ein (sobald die Properties live sind).
 */
window.JH_TRACKING = {
  // Meta Business Manager → Pixel-ID (15-stellig). Leer lassen = Pixel deaktiviert.
  metaPixelId: '',

  // GA4 Property → Measurement ID (Form: G-XXXXXXXXXX). Leer lassen = GA4 deaktiviert.
  ga4MeasurementId: ''
};
